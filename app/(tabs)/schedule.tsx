import { FC, memo } from 'react';
import { Text } from 'react-native';

const MorePage: FC = () => {
  return <Text className="text-red-800">Hello schedule😡</Text>;
};

export default memo(MorePage);
