export interface LoadingProps {
  // 加载提示文本
  text?: string;
  // 加载指示器颜色
  color?: string;
  // 加载指示器大小
  size?: 'small' | 'large';
  // 自定义容器样式
  containerStyle?: object;
  //自定义文本样式
  textStyle?: object;
}
